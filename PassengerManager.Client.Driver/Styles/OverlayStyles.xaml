<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:PassengerManager.Client.Driver.Controls">

    <Style x:Key="SosCircularStyle" TargetType="{x:Type controls:HoldToActivateButton}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource BrushRed}"/>
        <Setter Property="BorderThickness" Value="6"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="FontSize" Value="56"/>
        <Setter Property="Width" Value="80"/>
        <Setter Property="Height" Value="80"/>
        <Setter Property="Cursor" Value="Hand"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:HoldToActivateButton}">
                    <Grid Background="Transparent">
                        <Ellipse Fill="{TemplateBinding Background}"
                             Stroke="{TemplateBinding BorderBrush}"
                             StrokeThickness="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=BorderThickness.Left}"
                             Margin="-3"/>

                        <Ellipse Fill="{TemplateBinding BorderBrush}"
                             RenderTransformOrigin="0.5,0.5">
                            <Ellipse.RenderTransform>
                                <ScaleTransform ScaleX="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=HoldProgress}"
                                            ScaleY="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=HoldProgress}"/>
                            </Ellipse.RenderTransform>
                        </Ellipse>

                        <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" Value="0.8"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
