<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:PassengerManager.Client.Driver.Controls">

    <Style TargetType="controls:HoldToActivateButton" x:Key="SosButtonStyle" BasedOn="{StaticResource NumpadButtonStyle}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource BrushWhite}"/>
        <Setter Property="FontSize" Value="56"/>
        <Setter Property="FontFamily" Value="Noto Sans JP"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Width" Value="120"/>
        <Setter Property="Height" Value="120"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:HoldToActivateButton">
                    <Grid>
                        <Border Background="Transparent"
                                CornerRadius="70"
                                BorderBrush="{StaticResource BrushRed}"
                                BorderThickness="6"
                                Margin="-6"/>
                        <Grid ClipToBounds="True">
                            <Border CornerRadius="70" ClipToBounds="True">
                                <Rectangle Fill="{StaticResource BrushRed}"
                                           VerticalAlignment="Bottom"
                                           RenderTransformOrigin="0.5,1">
                                    <Rectangle.RenderTransform>
                                        <ScaleTransform ScaleX="1"
                                                        ScaleY="{Binding HoldProgress, RelativeSource={RelativeSource TemplatedParent}}" />
                                    </Rectangle.RenderTransform>
                                </Rectangle>
                            </Border>
                        </Grid>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
